services:
  - type: web
    name: hgss-school
    runtime: static
    buildCommand: corepack enable && corepack prepare pnpm@11.9.0 --activate && pnpm install --frozen-lockfile && pnpm --dir artifacts/hgss-school build
    staticPublishPath: artifacts/hgss-school/dist/public
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: BASE_PATH
        value: /
